export type DataPlants = {
  plants: DataPlants[];
};

export type DataPlant = {
  id: number;
  name: string;
  description: string;
  price: number;
  watches: number;
  image: string;
};

export type DataRadios = {
  radio: DataPlants[];
};

export type DataRadio = {
  id: number;
  image: string;
};

